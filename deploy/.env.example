# Non-sensitive defaults for local stack execution
TZ=Europe/Brussels
STARTER_TRANSPORT=http
PORT=7010

# Sensitive values are defined in one file only:
#   .secrets/jarvis.env (created from secrets.example.env)
# Required keys in that file: NPM_URL, NPM_IDENTITY, NPM_SECRET
